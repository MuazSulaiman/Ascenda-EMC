\# Streamlit + Render PostgreSQL — v11



\## Local prerequisites

\- Python 3.11+

\- Git

\- (Optional) `.env` or `.streamlit/secrets.toml` for local only



\## First-time GitHub push

```bash

git init

git add .

git commit -m "Initial commit: v11 Render deploy"

git branch -M main

git remote add origin https://github.com/<YOUR\_USERNAME>/streamlit-app-v11.git

git push -u origin main



